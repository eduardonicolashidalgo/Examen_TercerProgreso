<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Examen_TercerProgreso.Views.Detector_NH"
             Title="Detector_NH">
    <StackLayout>
        <!--<Label Text="Lista de nombres" FontSize="Title" Padding="30,10,30,10"/>
        <SearchBar x:Name="Buscador"
                   Placeholder="Buscador..."
                   TextColor="Black"/>-->
        <Button Text="Llama API" Clicked="Button_Clicked"/>
        <ListView x:Name="Lista">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding id}" Detail="{Binding email}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>